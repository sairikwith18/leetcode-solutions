class Solution {
public:
    bool check(vector<int>& nums) {
        if(nums.size()==1) return true;
        int c=0;
        for(int i=0;i<nums.size();i++){
            if(nums[i]>nums[(i+1)%nums.size()]) c=c+1;
            if(c>1) return false;
        }
        return true;
    }
};
